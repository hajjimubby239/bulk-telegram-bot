# bulk-telegram-bot
📝 README-ready summary
The project is a no-funds BULK Builder Codes simulator for Telegram. It generates builder-code payloads, stores approval data in SQLite, simulates execution, tracks virtual balances, and exports state for inspection. No real funds or wallet signing are required.

That is the right public-facing description for README.md.
